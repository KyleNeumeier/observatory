# Configure these only for local personal mode. Never put secrets in VITE_ variables.
AI_CHAT_ENDPOINT=
AI_MODEL=
AI_API_KEY=
# Conservative upper bound per request, based on provider token prices:
# up to 12,000 UTF-8 input bytes plus system message, and 256 output tokens.
# Calls are disabled until set. $5 total reservations per UTC month, no refunds.
AI_MAX_REQUEST_USD=0
